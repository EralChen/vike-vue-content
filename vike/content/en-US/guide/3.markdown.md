---
title: Writing Markdown
description: Basic Markdown syntax, code blocks, callouts, plus comark's MDC inline attributes and component syntax.
navigation:
  title: Writing Markdown
---

# Writing Markdown

Content in `vike-vue-content` is parsed by [comark](https://comark.dev). It's compatible with standard Markdown while also supporting MDC (MarkDown Components) extensions — letting you attach attributes and embed Vue components right in your Markdown.

## Basic syntax

All standard Markdown works out of the box:


```md
# Heading 1
## Heading 2

**bold**, *italic*, `inline code`, ~~strikethrough~~.

- Unordered list
- List item
  - Nested item

1. Ordered list
2. Second item

> Blockquote

[Link](https://vike.dev)
![Image](/logo.svg)

| Header | Header |
| --- | --- |
| Cell | Cell |
```

Headings are collected into the page's table of contents (TOC) and get anchor ids automatically.

## Code blocks

Wrap code in triple backticks and tag the language to enable syntax highlighting:

```ts
const msg: string = 'hello'
```

````md
```ts
const msg: string = 'hello'
```
````

## Callouts

GitHub-style callout syntax is supported:


> [!NOTE]
> An informational note.

> [!WARNING]
> A warning message.

```md
> [!NOTE]
> An informational note.

> [!WARNING]
> A warning message.
```

## MDC inline attributes

Attach attributes to an element with a trailing `{...}` block. Works on links, inline code, emphasis, and more:

[Emphasized link](https://vike.dev){.text-primary target="_self"}

`code`{.highlight}

[Some text]{#my-anchor .note}


```md
[Emphasized link](https://vike.dev){.text-primary target="_self"}

`code`{.highlight}

[Some text]{#my-anchor .note}
```

Syntax conventions:

- `{.class-name}` — add a class
- `{#id-name}` — add an id
- `{key="value"}` — add any attribute

The most common use is controlling how links open — see [Components · Link](/en-US/guide/components#link).

## MDC components

Use `::` syntax to embed Vue components in Markdown.

Block components (paired `::`):

::alert{type="warning"}
This goes into the component's default slot.
::

```md
::alert{type="warning"}
This goes into the component's default slot.
::
```

Inline components (`:name[content]{attrs}`):

This is a :badge[New]{.text-red} badge.

```md
This is a :badge[New]{.text-red} badge.
```

Component names map to Vue components you register via `ContentRenderer`'s `components` map — see [Components · ContentRenderer](/en-US/guide/components#contentrenderer).

## Next steps

- [Frontmatter](/en-US/guide/frontmatter) — page-level metadata
- [Components](/en-US/guide/components) — Link, ContentRenderer, and more
