## 打包

```shell
cd entry
pnpm build
```

## 发布

```shell
cd entry
npm pack --dry-run
npm publish --access public --registry https://registry.npmjs.org/
```

## Prompt

## 对标

对标 .dev/content 的xx功能。先读完源码，列出关键实现细节和数据流，给我看，确认后再写代码。
